{
	"build": ["npm install", "npm run build", "grunt", "composer install --no-dev"],
	"package": {
		"prefix": "gravity-forms-zero-spam",
		"includeExtra": ["dist"]
	},
	"release": {
		"branch": "main",
		"mergeTo": "develop"
	}
}
