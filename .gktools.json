{
	"build": ["npm run build", "composer install --no-dev"],
	"package": {
		"prefix": "gravity-forms-zero-spam",
		"includeExtra": ["dist"]
	},
	"release": {
		"branch": "main",
		"mergeTo": "develop"
	}
}
